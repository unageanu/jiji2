machine:
  ruby:
    version: 2.2.3
  node:
    version: 0.10.34

dependencies:
  pre:
    - gem install rubygems-bundler
    - gem regenerate_binstubs
  post:
   - npm install:
       pwd:
         sites

test:
  override:
    - gem -v
    - bundle -v
    - bundle exec bundle -v
#    - bundle exec rake
#    - npm run-script build:
#        pwd:
#          sites
