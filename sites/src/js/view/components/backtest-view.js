import React      from "react";
import MUI        from "material-ui";

export default React.createClass({
  render() {
      return (
          <div>
            バックテスト
          </div>
      );
  }
});
